spring:
# 별도의 설정이 없으면 spring 에서 알아서 메모리 모드로 실행하기 때문에 설정하지 않아도 동일함.
#  datasource:
#    # 테스트 코드에서는 메모리모드로 사용
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create-drop # 테스트에서는 디폴트로 create-drop
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace